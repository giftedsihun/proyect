# Secure Message 프로젝트 설명 및 계획

## 1. 프로젝트 한눈에 보기

**Secure Message**는 고등학교 이산수학 개념을 실제 정보 전송 과정에 연결한 브라우저 기반 보안 통신 웹앱이다. 텍스트 또는 파일을 입력하면 브라우저 안에서 허프만 압축과 SHA-256 기반 XOR 암호화를 수행하고, 복호화와 무결성 검증까지 체험할 수 있다.

프로젝트의 핵심 질문은 다음과 같다.

> 트리, 부울대수, 그래프 같은 이산수학 구조는 실제로 안전하고 효율적인 데이터 전달에 어떻게 사용될 수 있는가?

## 2. 구현 완료 기능

### 2.1 암호화와 복호화

- 텍스트 메시지 또는 최대 25MB 파일을 입력할 수 있다.
- 각 바이트의 출현 빈도를 계산해 허프만 트리를 생성하고 무손실 압축한다.
- 사용자가 입력한 비밀 키를 SHA-256으로 32바이트 키 재료로 확장한다.
- 압축 데이터와 키 재료에 XOR 연산을 적용해 암호문을 만든다.
- 원본의 SHA-256 해시를 저장하고, 복호화 후 다시 계산해 파일 손상이나 잘못된 키를 확인한다.
- 파일 이름, MIME 타입, 원본 크기, 빈도표, 무결성 해시, 암호문을 자체 `.enc` 패키지 형식으로 저장한다.

### 2.2 이산수학 시각화

- **트리:** 허프만 트리와 각 간선의 0/1 코드를 화면에 표시한다.
- **부울대수:** `A XOR K XOR K = A`라는 XOR의 자기역원 성질을 복호화 과정과 연결한다.
- **그래프:** 중계 서버를 정점, 네트워크 연결을 간선으로 모델링한다.
- **다익스트라 알고리즘:** 전송 시간 또는 위험도를 가중치로 선택해 최소 비용 경로와 탐색 과정을 보여 준다.
- **바이트 분포:** 원본, 암호문, 단순 반복 키 방식의 바이트 빈도 그래프를 비교한다.

### 2.3 암호문 공유

- `.enc` 파일을 직접 다운로드해 메신저, 이메일, USB 등으로 전달할 수 있다.
- 선택적으로 Supabase Storage와 Database에 암호문 패키지와 최소 메타데이터를 올릴 수 있다.
- 공유 보관함에서 만료되지 않은 암호문을 받아 복호화 화면으로 바로 가져올 수 있다.
- 서버에 원본 파일이나 비밀 키를 전송하지 않는다.

## 3. 기술 구성

| 구분 | 사용 기술 | 역할 |
| --- | --- | --- |
| 화면 | HTML, CSS, Vanilla JavaScript | 반응형 보안 통신 웹 UI |
| 압축 | 허프만 코딩, 최소 힙 | 바이트 빈도 기반 무손실 압축 |
| 암호 | Web Crypto API SHA-256, XOR | 교육용 암호화와 무결성 검증 |
| 경로 | 가중치 그래프, 다익스트라 | 시간/위험도 최소 경로 계산 |
| 공유 | Supabase REST API, Storage | 암호문 패키지 저장과 목록 제공 |
| 자료 | Python, python-pptx, Pillow | 발표 PPT와 포스터 생성 |

## 4. 파일 구성

| 파일 또는 폴더 | 내용 |
| --- | --- |
| `index.html` | 암호화, 복호화, 경로, 분석, 공유 보관함 UI |
| `style.css` | 보안 대시보드 스타일과 반응형 레이아웃 |
| `script.js` | 압축, XOR, SHA-256, `.enc`, 그래프, Supabase 연동 |
| `supabase-setup.md` | Supabase 버킷, 테이블, RLS 정책 설정 안내 |
| `presentation/secure-message-presentation.pptx` | 12장 한국어 발표 자료 |
| `presentation/secure-message-poster.png` | A4 세로 포스터 |
| `presentation/create_presentation.py` | PPT와 포스터 재생성 스크립트 |

## 5. 사용 방법

1. `index.html`을 브라우저로 연다.
2. 텍스트 또는 파일과 비밀 키를 입력한 후 **암호화 시작**을 누른다.
3. 생성된 허프만 트리와 압축 결과를 확인하고 `.enc` 파일을 다운로드한다.
4. 같은 비밀 키와 `.enc` 파일을 복호화 탭에 입력해 원본과 SHA-256 검증 결과를 확인한다.
5. 공유 기능을 사용할 경우 `supabase-setup.md`의 절차로 Supabase를 설정한다.

## 6. 발표에서 강조할 점

- 단순히 수학 개념을 설명한 것이 아니라, 각 개념을 실제 프로그램 기능으로 구현했다.
- 허프만 트리는 데이터 표현의 효율, XOR은 비트 연산 기반 기밀성의 원리, 다익스트라는 전달 경로 최적화를 담당한다.
- 원본과 키를 브라우저에 남기고 암호문만 공유하도록 설계해 데이터 최소화 원칙을 보여 준다.
- 바이트 분포 그래프와 가중치 변경 실험으로 알고리즘의 결과를 직접 확인할 수 있다.

## 7. 보안 한계

현재 XOR 방식은 **이산수학 학습용 구현**이다. SHA-256 결과 32바이트를 반복해 XOR하는 방식은 실제 서비스에서 사용하는 검증된 암호 방식이 아니다. 실제 서비스로 발전시키려면 다음이 필요하다.

- 전송마다 새로운 nonce/IV를 사용하는 AES-GCM 같은 인증 암호
- 강한 비밀번호 처리와 키 파생 함수(PBKDF2, Argon2 등)
- 사용자 로그인과 파일별 권한 관리
- 업로드 횟수 제한, 파일 검사, 악성 사용 대응
- 만료 파일 자동 삭제와 감사 로그

## 8. 향후 개발 계획

### 단기 계획

1. 실제 브라우저에서 암호화 → Supabase 업로드 → 보관함 수신 → 복호화 전체 흐름을 시연한다.
2. 발표 화면에서 사용할 예시 텍스트와 파일을 준비한다.
3. 허프만 압축률, 패키지 크기, 경로 비용의 실험 결과를 표로 정리한다.
4. GitHub Pages 등 정적 호스팅으로 웹앱 접근 주소를 제공한다.

### 중기 계획

1. 공개 보관함 대신 공유 코드 또는 수신자 지정 방식을 도입한다.
2. 만료된 Storage 객체를 정리하는 자동 삭제 기능을 추가한다.
3. 파일의 원래 이름을 공유 메타데이터에 안전하게 반영한다.
4. 다익스트라 단계별 애니메이션과 경로별 비용 비교를 더 자세히 제공한다.

### 장기 계획

1. 교육용 XOR 방식을 Web Crypto API의 AES-GCM으로 확장하고 두 방식의 차이를 비교한다.
2. 로그인, 접근 제어, 비율 제한을 갖춘 별도 백엔드 API를 설계한다.
3. 학습자가 직접 빈도표, 키 길이, 그래프 가중치를 바꾸며 결과를 기록하는 실험 모드를 만든다.

## 9. 결론

Secure Message는 트리, 부울대수, 그래프라는 서로 다른 이산수학 개념이 실제 데이터 전송의 **압축**, **변환**, **이동** 문제를 해결하는 모습을 하나의 웹앱에서 보여 준다. 이 프로젝트를 통해 수학적 구조가 코드와 사용자 경험으로 연결되는 과정을 확인할 수 있다.
