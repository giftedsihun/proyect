# Secure Message 프로젝트 설명 및 계획

## 1. 프로젝트 한눈에 보기

**Secure Message**는 이산수학 개념을 데이터 처리 과정에 적용한 브라우저 기반 보안 통신 웹앱이다. 텍스트 또는 파일을 입력하면 브라우저 안에서 허프만 압축과 PBKDF2 기반 AES-256-GCM 암호화를 수행하고, 복호화와 변조 검증까지 확인할 수 있다. XOR 방식은 부울대수 비교를 위한 선택 모드로 제공한다.

프로젝트의 핵심 질문은 다음과 같다.

> 트리와 부울대수 같은 이산수학 구조는 실제로 안전하고 효율적인 데이터 처리에 어떻게 사용될 수 있는가?

## 2. 구현 완료 기능

### 2.1 암호화와 복호화

- 텍스트 메시지 또는 최대 25MB 파일을 입력할 수 있다.
- 각 바이트의 출현 빈도를 계산해 허프만 트리를 생성하고 무손실 압축한다.
- 기본 모드는 매 암호화마다 새 salt와 96비트 IV를 생성하고, PBKDF2-SHA-256 250,000회로 AES-256 키를 유도한다.
- AES-GCM으로 압축 데이터를 암호화하며, 암호문과 패키지 헤더의 변조를 함께 검증한다.
- 새 `.enc` 패키지(SME2)는 salt, IV, PBKDF2 반복 횟수, 허프만 복원 정보와 AES-GCM 암호문을 저장한다.
- 기존 SHA-256 반복 XOR 패키지(SME1)는 호환을 위해 계속 복호화할 수 있으며, 새로 만들 때는 학습용 선택 모드에서만 사용할 수 있다.

### 2.2 이산수학 시각화

- **트리:** 허프만 트리와 각 간선의 0/1 코드를 화면에 표시한다.
- **부울대수:** 학습용 XOR 모드에서 `A XOR K XOR K = A`라는 자기역원 성질을 복호화 과정과 연결하고, AES-GCM 기본 모드와 한계를 비교한다.
- **바이트 분포:** 원본, 암호문, 단순 반복 키 방식의 바이트 빈도 그래프를 비교한다.

### 2.3 암호문 공유

- `.enc` 파일을 직접 다운로드해 메신저, 이메일, USB 등으로 전달할 수 있다.
- 선택적으로 Supabase Storage와 Database에 암호문 패키지와 최소 메타데이터를 올릴 수 있다.
- 공유 보관함에서 만료되지 않은 암호문을 받아 복호화 화면으로 바로 가져올 수 있다.
- 서버에 원본 파일이나 비밀 키를 전송하지 않는다.

## 3. 기술 구성

| 구분 | 사용 기술 | 역할 |
| --- | --- | --- |
| 화면 | HTML, CSS, Vanilla JavaScript | 반응형 보안 통신 웹 UI |
| 압축 | 허프만 코딩, 최소 힙 | 바이트 빈도 기반 무손실 압축 |
| 암호 | Web Crypto API PBKDF2, AES-256-GCM, SHA-256, XOR | 기본 인증 암호화와 XOR 학습 비교 |
| 공유 | Supabase REST API, Storage | 암호문 패키지 저장과 목록 제공 |
| 자료 | Python, python-pptx, Pillow | 발표 PPT와 포스터 생성 |

## 4. 파일 구성

| 파일 또는 폴더 | 내용 |
| --- | --- |
| `index.html` | 암호화, 복호화, 분석, 공유 보관함 UI |
| `style.css` | 보안 대시보드 스타일과 반응형 레이아웃 |
| `script.js` | 압축, PBKDF2, AES-GCM, XOR 학습 모드, `.enc`, 바이트 분포, Supabase 연동 |
| `supabase-setup.md` | Supabase 버킷, 테이블, RLS 정책 설정 안내 |
| `presentation/secure-message-presentation.pptx` | 12장 한국어 발표 자료 |
| `presentation/secure-message-poster.png` | A4 세로 포스터 |
| `presentation/create_presentation.py` | PPT와 포스터 재생성 스크립트 |

## 5. 사용 방법

1. `index.html`을 브라우저로 연다.
2. 텍스트 또는 파일과 비밀 키를 입력한 후 **암호화 시작**을 누른다.
3. 생성된 허프만 트리와 압축 결과를 확인하고 `.enc` 파일을 다운로드한다.
4. 같은 비밀 키와 `.enc` 파일을 복호화 탭에 입력해 원본과 AES-GCM 인증 검증 결과를 확인한다.
5. 공유 기능을 사용할 경우 `supabase-setup.md`의 절차로 Supabase를 설정한다.

## 6. 발표에서 강조할 점

- 단순히 수학 개념을 설명한 것이 아니라, 각 개념을 실제 프로그램 기능으로 구현했다.
- 허프만 트리는 데이터 표현의 효율, XOR은 비트 연산의 원리 비교, AES-GCM은 실제 인증 암호화를 담당한다.
- 원본과 키를 브라우저에 남기고 암호문만 공유하도록 설계해 데이터 최소화 원칙을 보여 준다.
- 허프만 트리와 바이트 분포 그래프로 알고리즘의 결과를 직접 확인할 수 있다.

## 7. 보안 범위와 한계

기본 모드는 PBKDF2-SHA-256과 AES-256-GCM을 사용한다. 매 암호화마다 새로운 salt와 IV를 만들고, AES-GCM의 인증 태그로 암호문과 패키지 헤더의 변경을 감지한다. 다만 이 앱은 로그인과 수신자 권한 관리가 없는 브라우저 기반 교육 프로젝트이므로 완전한 메신저 서비스는 아니다.

XOR 모드는 **이산수학 학습용 구현**이다. SHA-256 결과 32바이트를 반복해 XOR하는 방식은 실제 서비스에서 사용하는 검증된 암호 방식이 아니며, 같은 키의 반복 사용과 키 추측 공격에 취약하다. 실제 서비스로 발전시키려면 다음이 필요하다.

- 전송마다 새로운 nonce/IV를 사용하는 AES-GCM 같은 인증 암호
- 강한 비밀번호 처리와 키 파생 함수(PBKDF2, Argon2 등)
- 사용자 로그인과 파일별 권한 관리
- 업로드 횟수 제한, 파일 검사, 악성 사용 대응
- 만료 파일 자동 삭제와 감사 로그

## 8. 향후 개발 계획

### 단기 계획

1. 실제 브라우저에서 암호화 → Supabase 업로드 → 보관함 수신 → 복호화 전체 흐름을 시연한다.
2. 발표 화면에서 사용할 예시 텍스트와 파일을 준비한다.
3. 허프만 압축률과 패키지 크기의 실험 결과를 표로 정리한다.
4. GitHub Pages 등 정적 호스팅으로 웹앱 접근 주소를 제공한다.

### 중기 계획

1. 공개 보관함 대신 공유 코드 또는 수신자 지정 방식을 도입한다.
2. 만료된 Storage 객체를 정리하는 자동 삭제 기능을 추가한다.
3. 파일의 원래 이름을 공유 메타데이터에 안전하게 반영한다.
4. 만료된 Storage 파일을 정리하는 서버 측 자동 삭제 작업을 추가한다.

### 장기 계획

1. AES-GCM 기본 모드와 교육용 XOR 모드의 보안성, 패키지 크기, 처리 시간을 비교하는 실험 결과를 추가한다.
2. 로그인, 접근 제어, 비율 제한을 갖춘 별도 백엔드 API를 설계한다.
3. 학습자가 직접 빈도표, 키 길이, 그래프 가중치를 바꾸며 결과를 기록하는 실험 모드를 만든다.

## 9. 결론

Secure Message는 트리와 부울대수라는 이산수학 개념이 데이터의 **압축**과 **변환** 문제를 해결하는 모습을 하나의 웹앱에서 보여 준다. 실제 네트워크·내비게이션에서의 다익스트라 알고리즘은 별도의 활용 사례로 설명할 수 있지만, 현재 앱의 전송 경로 기능으로는 구현하지 않는다. 이 프로젝트를 통해 수학적 구조가 코드와 사용자 경험으로 연결되는 과정을 확인할 수 있다.
