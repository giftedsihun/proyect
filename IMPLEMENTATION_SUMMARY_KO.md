# Secure Message 구현 및 변경 사항

## 프로젝트 목적

Secure Message는 이산수학의 데이터 표현과 비트 연산 개념을 적용해, 텍스트 또는 파일을 브라우저에서 압축·암호화하고 `.enc` 패키지로 공유·복원하는 웹앱이다.

현재 실제 앱의 처리 흐름은 다음과 같다.

```text
원본 입력 -> 허프만 압축 -> PBKDF2 키 유도 -> AES-256-GCM 암호화
-> SME2 .enc 패키지 생성 -> 다운로드 또는 Supabase 공유 -> 인증 복호화 -> 원본 복원
```

## 구현된 기능

### 1. 허프만 무손실 압축

- 텍스트와 최대 25MB 파일의 바이트 빈도표를 계산한다.
- 최소 힙으로 빈도가 낮은 두 노드를 반복 결합해 허프만 이진 트리를 만든다.
- 접두어 없는 이진 코드로 데이터를 압축하고, 트리와 코드표를 화면에 표시한다.
- 압축 전후 크기와 바이트 분포를 확인할 수 있다.

### 2. 기본 암호화: PBKDF2 + AES-256-GCM

- 새 암호화마다 16바이트 salt와 12바이트 IV를 새로 생성한다.
- 사용자가 입력한 비밀번호는 PBKDF2-SHA-256 250,000회로 AES-256 키를 유도한다.
- AES-256-GCM으로 압축 데이터를 암호화하고, 패키지 헤더를 추가 인증 데이터(AAD)로 포함한다.
- 비밀번호가 틀리거나 암호문·헤더가 바뀌면 인증 검증에 실패하여 복호화를 거부한다.

### 3. `.enc` 패키지와 호환성

- 새 패키지 형식 `SME2`에는 파일 이름, MIME 형식, 원본 크기, 허프만 복원 정보, PBKDF2 반복 횟수, salt, IV, AES-GCM 암호문과 인증 태그가 포함된다.
- 기존 `SME1` 패키지는 복호화 호환성을 유지한다.
- 새 파일의 기본값은 `SME2 / AES-256-GCM`이며, `SME1 / SHA-256 반복 XOR`은 비교용 선택 모드에서만 생성된다.

### 4. XOR 비트 연산 비교

- `A XOR K XOR K = A`의 자기역원 성질을 확인하는 비교 모드를 제공한다.
- 단순 반복 키와 SHA-256으로 확장한 반복 키의 바이트 분포를 비교한다.
- 반복 XOR은 실제 파일 보호에 적합하지 않으므로 기본 암호 방식으로 사용하지 않는다.

### 5. 암호문 공유 보관함

- 암호화된 `.enc` 패키지만 Supabase Storage에 업로드한다.
- 원본 파일과 비밀번호는 브라우저 밖으로 전송하지 않는다.
- 보관함에서 암호문을 받아 복호화 화면으로 가져올 수 있다.

## 이산수학과의 연결

| 개념 | 앱에서의 적용 |
| --- | --- |
| 이진 트리 | 허프만 트리로 바이트별 접두어 없는 코드를 생성 |
| 빈도표 | 0~255 바이트의 출현 횟수를 계산해 압축과 분포 분석에 사용 |
| 부울대수 XOR | 비트 단위 XOR의 자기역원 성질을 비교·시각화 |
| 이진 데이터 표현 | 텍스트·파일·압축 비트열·`.enc` 패키지를 바이트 단위로 처리 |
| 최소 힙 | 허프만 트리를 효율적으로 구성 |

다익스트라 알고리즘은 현재 앱의 실제 업로드·공유 경로를 계산하거나 제어하지 않는다. 실제 활용 사례로는 내비게이션과 OSPF 라우팅의 최단 경로 계산을 설명할 수 있지만, 앱의 구현 기능으로 발표하면 안 된다.

## 최근 변경 사항

1. 기존 SHA-256 반복 XOR 기본 암호화를 PBKDF2 + AES-256-GCM 기본 모드로 교체했다.
2. AES-GCM 인증 검증과 `SME2` 패키지 형식을 추가하고, 기존 `SME1` 복호화 호환성을 유지했다.
3. 암호화 방식 선택 UI, 발표용 예시 데이터, AES-GCM 복호화 안내를 추가했다.
4. 실제 통신과 연결되지 않던 중계 서버·다익스트라 경로·패킷 전송 시뮬레이션 UI와 JavaScript를 앱에서 제거했다.
5. PPT와 포스터를 AES-GCM 기본 암호화와 실제 사례 중심 내용으로 갱신했다.

## 보안 범위와 개선 방향

- Supabase 보관함은 현재 수업 시연용 공개 정책을 전제로 한다. URL을 아는 사람은 암호문 목록과 파일에 접근할 수 있지만, 별도 비밀번호 없이는 원본을 복호화할 수 없다.
- 만료된 항목은 목록에서 숨겨지지만 Storage 파일까지 자동 삭제되지는 않는다.
- 실제 서비스로 확장하려면 로그인·수신자별 권한, 업로드 제한, 악성 파일 검사, 감사 로그, 만료 파일 자동 삭제를 추가해야 한다.
- 파일 자동 삭제는 브라우저가 아니라 Supabase Edge Function과 Cron 같은 서버 측 예약 작업으로 구현해야 한다. `service_role` 키는 절대 브라우저 코드에 넣지 않는다.

## 검증 기록

- `node --check script.js` 통과
- `git diff --check` 통과
- Node Web Crypto 기반 AES-GCM 패키지 왕복 복호화 및 변조 거부 테스트 통과
- 로컬 HTTP 서버에서 AES-GCM UI 제공과 중계 경로 UI 제거 확인
